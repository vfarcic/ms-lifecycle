- hosts: swarm
  remote_user: vagrant
  serial: 1
  sudo: yes
  vars:
    - debian_version: vivid
    - docker_cfg_dest: /lib/systemd/system/docker.service
    - is_systemd: true
    - skip_ui: true
  roles:
    - common
    - docker
    - consul-healing
    - swarm
    - registrator
